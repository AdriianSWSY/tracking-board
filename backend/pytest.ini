[pytest]
python_classes =
    *TestCase
    Test*
python_files = tests.py test_*.py *_tests.py
testpaths =
    apps
addopts =
    -p no:warnings
    --ds=config.settings.dev
    --maxfail=2
    --durations=5
    -rfsExX
    --reuse-db
